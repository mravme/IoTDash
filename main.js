let ctrl = require('./mBetGrbr');
let data = require('./dataLoop');
let client = require('./conn/connServer');
async function main(){
        loop=true;
        do{
        }while(loop);
}
main();